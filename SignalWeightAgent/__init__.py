"""Signal Weight Agent — independence detection + ensemble weighting.

Agent package (SKILL.md, launcher, reports). The deterministic core lives in
strategy_framework/analysis/ (signal_ensemble.py, signal_study.py) — shared with
api.signal_correlation. See SKILL.md.
"""
