# Hypotheses — single source of truth for backtesting results

**Read this before testing anything.** Most ideas in this file have already been
tested and killed. Re-running a dead hypothesis wastes a session and, worse,
tends to resurrect it — the second run finds a different arbitrary threshold and
reports it as new.

Every row links to the script that produced it and the result JSON it wrote. If a
row says DEAD, the burden is on the new test to explain what is different, not to
re-derive the same number.

- **Scope:** all hypothesis testing on this repo through 2026-08-13.
- **Location rule:** this file is the only place backtesting verdicts live.
  Scripts and `*_result.json` stay at repo root; verdicts are recorded here.
- **Update rule:** a test is not finished until its row is in the register below.

---

## 0. BEFORE YOU REPORT A DATA PROBLEM — READ THIS TABLE

Every row below is a symptom that ALREADY HAS AN ANSWER. Most of them look exactly like a
fresh defect: a mismatch between two stores, a zero where a number belongs, a count that does
not tie, a value outside a plausible band.

WHY THIS SECTION EXISTS. On 2026-08-17 an assistant reported four "findings" in one session
that were all artifacts of how it looked, not defects in the data: a cross-join that omitted
`basis`/`time_period`/`section` and manufactured conflicts; a Postgres table identified from an
unqualified `relname`, producing a confident and wrong "correction" to O15; a lot-size checker
flagging APOLLOHOSP for ordinary price drift; and a two-store comparison that recommended
harvesting a copy already diagnosed and fixed weeks earlier. Each cost real time to re-litigate.
The pattern was identical every time — **a set difference or a threshold applied without the
dimension that explains it** — and in every case the answer was already written down here.

So the rule is not "read the whole register", which is impractical at this length. It is: **scan
this table for your symptom before saying the word mismatch.** If it is here, it is settled;
follow the pointer rather than re-deriving it. If it is genuinely not here, say so explicitly —
"not in the symptom index" — which is also how this table grows.

Facts live in the sections below; this table holds only symptoms and pointers, so it cannot
drift out of step with them.

| symptom you might observe | already answered | where |
|---|---|---|
| Two stores hold `fii_dii_flows` and disagree; Postgres has Sunday rows; ~126 values differ | Pre-fix residue. Sundays are a documented Upstox quirk `_flow_date()` drops; the differences are ONE offset counted many times. SQLite is correct | §3, and `data_agent/quality/fii_store_compare.py` |
| FII F&O net columns are `0.0` on 2026-06-18 and 2026-06-22 | Zero in BOTH stores — the F&O feed's start boundary, not lost fetches. Settled, do not re-check | §3 |
| An FII cash flow reads exactly `0.0` on some other date | `flows_cash_cache.json` writes 0.0 on fetch failure and zero is legitimate for a NET flow. 2026-07-17 confirmed wrong (true −216,528) | §3 |
| ICICIBANK's FII stake jumps ~15pp in one quarter | Vendor CLASSIFICATION dispute, not an arithmetic error. Nothing to repair | §3, and `quality_growth.py` docstring |
| An FII period label is not a quarter-end (`Jul 2026`, `Apr 2026`) | Vendor label defect. Direction usable, magnitude not. Flag only, never a gate | §3 |
| Postgres `companies` has 102 rows but the universe is 62 | 62 real + 40 `DUMMY_ISIN_*`. **DO NOT DELETE the dummies** — they are a second convention, not duplicates | O15 |
| `financials` has conflicting rows for the same company and period | Two conventions split by LINE ITEM (revenue matches one, net_profit the other). Reconcile, never drop a side. The join MUST include `basis`, `time_period` AND `section` | O15 |
| A table named `fundamentals` has only 5 rows | `fundamentals` is a **SCHEMA** (`schema.sql:21`); the real table is `fundamentals.financials`. The 5-row stray is not what O15 is about. Never identify a PG table from an unqualified name | O15 |
| `delivery_history.json` covers fewer names than the page scrape | Known: the Excel export lags. The quarterly series comes from `attributable_panel.json`; the screen's gates read the ANNUAL series | C34, `data_agent/freshness.py` |
| Two tabs quote different aggregate profit growth | Was real, now fixed — both resolve through `_quarters_source()`, which prints which source it used | C34 |
| Aggregate growth disagrees with a broker's number | **Check BASIS before universe.** Brokers publish NORMALISED (ex-one-off) aggregates; every number in this repo is REPORTED. On Q1 FY27 that one distinction is worth ~4.7pp on its own, and the composition agrees on 8 of 9 named companies — so a large gap is usually the same companies measured on a different basis, not a different index | C31, C38, O14 |
| A broker's SECTOR call needs checking | The panel tests it directly — Q1 FY27 attributable YoY by group, against their stance. Do it before arguing with the narrative: on BofA's 2026-08-18 note four of seven testable calls were supported by our earnings, one was supported MORE strongly than their own valuation rationale (healthcare, earnings **-12.3%**), one ran the other way on trailing data (telecom **+25.5%**), and one absolute claim failed outright (real estate 'sharp re-rating', actually **+3.5% YTD**). Their central recommendation was UNTESTABLE — mid-caps are not in the Nifty 50 | C40, `attributable_panel.json` |
| `expectation_snapshots.json` count exceeds distinct captures | Duplicate write from a concurrent run. Deduped; guard added; count is no longer proof | C36 |
| A futures `open_interest` looks impossibly large | OI and volume are quoted in **SHARES**, not contracts. Notional is `open_interest × close` with no lot multiplier | O12 |
| A lot size is not a round number (ADANIENT 309) | Correct. NSE sets lots to a contract-VALUE band, not to round numbers | O12, `lot_sizes.py` |
| `lot × price` sits outside ₹5–10 lakh | Price drift since the last lot revision. The band applies AT REVISION. Not a wrong lot | `lot_sizes.py` |
| `contract_size` is NULL on futures bars | Set by the WRITER at capture. The repo-local mirror predates that and will read NULL | C37 |
| Today's 1d futures bar is missing | Breeze publishes it in an overnight batch (~23:30–00:00 IST). Run the sync in the MORNING for the previous session | `freshness.py`, daily checklist |
| ~88 modules hardcode `option_chains.db` | READERS, which CLAUDE.md permits. Only a WRITER doing it is a defect | C37, `data_agent/quality/db_path_audit.py` |
| A number traced to `option_chains.db` looks stale | Two copies exist. Drive is the source of truth; the repo file is a manual mirror, only as fresh as the last `cp` | §3, `db_config.py` |
| A Postgres dump on Drive is older than the live database | Normal and expected. Postgres flows **local → Drive**: localhost is authoritative and Drive holds point-in-time dumps only. Staleness of the dump is its age, not a divergence | CLAUDE.md two-store directions |
| The repo-local `option_chains.db` is NEWER than the Drive copy | **A rule violation, not staleness.** SQLite flows **Drive → local**, so something wrote to the read-only mirror (C37). What it wrote is not in the source of truth and the next `cp` destroys it. Find the writer BEFORE refreshing | C37, `db_path_audit.py`, `backup_audit.py` |
| Some symbols have a 1d bar dated TODAY and others do not | **Vendor difference, not a gap.** Measured mid-session 2026-08-18: India/Yahoo 123 of 125 already had today's bar, MCX/Upstox 3 of 25, US indices 0 of 6. Yahoo's daily bar UPDATES through the session; Upstox's daily endpoint is end-of-day; Breeze publishes overnight. So price_bars holds live-partial and not-yet-written bars for the same date with nothing in the row to tell them apart — any cross-sectional calc on the latest date compares a half-finished bar against an absent one. `freshness.py --market` prints the split | `--market` |
| An index shows zero volume | Normal for INDIAVIX, VIX_US, SOX, GIFTNIFTY, USDINR and the NIFTY* sector indices — they report zero always. Volume is a usable partial-bar detector for EQUITIES (RELIANCE 9.95m at midday against 13.38m the prior full session) and is useless for indices | `--market` |
| A series steps by exactly 10x or 0.1x at a date boundary | **Suspect our own compensation before suspecting the market.** `sync_commodities` divided USDINR by 10 because Upstox quoted the GLOBAL_INDICATOR feed 10x scaled; on 2026-08-16 Upstox stopped scaling, the divisor stayed, and 95.5 was stored as 9.55. The test is arithmetic, not judgement — the ratio across the boundary was **10.0000 to four places** on a rate that moves 0.05% a day, which no market produces. Then ask how far the resume OVERLAP dragged it backwards: a 5-day overlap REPLACED five previously-correct daily bars, so the damage predates the vendor change | C39, `data_agent/quality/fix_usdinr_scale.py` |
| **NIFTY volume went 267,700 -> 0 on 2026-08-17 — UNDIAGNOSED** | Not partial-bar: 17-Aug is a COMPLETED session and still reads zero, and 18-Aug too, while every session back to 03-Aug carries 250k-355k. Not multiple-owner either — sync_coverage lists NIFTY under one job (`sync_nifty50_bars_yf._EXTRA`). Most likely a vendor-side change in what Yahoo returns for the index, but that is a HYPOTHESIS and was not verified. Volume on NIFTY should not be trusted across 17-Aug until someone checks | open |
| A downloader resumes by DATE while another resumes by TIMESTAMP | **Not an inconsistency — the vendor decides.** Breeze accepts `from_date=...T10:01:00.000Z` so sync_nifty50_to_now resumes at the next minute; Upstox's URL is `/historical-candle/{key}/1minute/{to}/{frm}`, dates only, so sync_commodities truncates and pairs it with a separate intraday endpoint. Resume at the finest granularity THE SOURCE ACCEPTS. Do not 'fix' the date one | `fo_bars.resume_from` |
| Unsure which way to copy between local and Drive | **The two stores are OPPOSITE, and each wrong direction is the right one for the other store.** SQLite: Drive → local (`cp`). Postgres: local → Drive (`pg_backup.sh`). Never the reverse — one overwrites the source of truth with a stale mirror, the other discards every ingest since the dump | CLAUDE.md two-store directions |
| The index P/E percentile looks extreme (3rd vs 10th) | Our reconstruction's EPS steps once a year while NSE's rolls quarterly, so the error FLIPS SIGN with the growth rate. No scalar deflator fixes it | C27 |
| A 1d close differs from the last 1m close | Market-structure break: NSE's Closing Auction Session launched 2026-08-03. Closes before and after are not comparable | §3 |
| A quality-screen name has a huge 5y CAGR (ICICI 47%) | CAGR inherits its endpoints; FY2021 is the bottom of the lender NPA cycle. Read the median beside it — the GAP is the base effect | C35 |
| Reliance clears a growth gate by a small margin | An 8,924 cr Asian Paints gain sits in the base. Reported ≠ underlying, everywhere in this repo | C33, O14 |
| JIOFIN / NESTLEIND / TATAMOTORS missing from the quality screen | **A deliberate gate, not a defect — and NOT an alias problem, which an earlier version of this row claimed.** Their workbooks exist and parse; `delivery_history.build()` returns None because `_MIN_YEARS = 6` and they hold 2, 4 and 4 annual P&L columns. The gate's own comment: *"fewer periods than this and consistency is an anecdote"*. Short history is expected — JIOFIN listed Aug-2023, TATAMOTORS reports post-demerger, NESTLEIND changed its fiscal year end (per-name causes inferred from the column counts, not separately verified). Nothing to fix unless the gate itself should change | `delivery_history.py:224`, `freshness.py` |

---

## 0. Method standard

Every result in this file was produced under these rules. A test that skips them
is not comparable to the rows here and should not be added.

| rule | why |
|---|---|
| **Null by circular shift, not permutation** | Plain permutation destroys autocorrelation and understates the null. Persistent regressors manufacture significance against a shuffled null. |
| **max-\|t\| across the whole signal family** | Testing 8 signals is 8 shots at one dataset. The yardstick is the distribution of the *largest* \|t\| under the null, with cross-correlation preserved (shift the block together). |
| **Newey-West standard errors (lag 5)** | OLS SEs are far too small on overlapping/persistent series. |
| **Non-overlapping windows for multi-day horizons** | Overlapping 20-day forward returns on daily data inflate n by ~20x. Report the non-overlapping n as the honest sample size. |
| **Publication lag is part of the signal** | NSE participant data for day D publishes after the close on D. Target must be D+1 or later. Anything that captures close(D)→open(D+1) is untradable by construction. |
| **Count tests and max tests answer different questions** | max-\|t\| is blind to a diffuse effect spread over many weak cells; a count test is blind to one strong effect. Run both when the alternative is unclear. |
| **Cross-sectional dependence breaks the 5% yardstick** | 11 sectors on shared regressors are not independent. Null median survivor count was 15, not the binomial 6.6. |
| **State the minimum detectable effect** | With n=247 and daily σ≈0.98%, min detectable r ≈ 0.179. Below that, a null means "cannot see", not "nothing there". |

---

## 1. Register

Verdicts: **DEAD** (no effect survives its null) · **SURVIVES** (clears its null; caveat noted) ·
**ARTIFACT** (effect real in data, caused by measurement flaw) · **RETRACTED** (reported, then failed a later test) ·
**MEASURED** (descriptive quantity, not a hypothesis) · **UNDERPOWERED** (cannot distinguish) · **OPEN** (untested)

### Sector rotation

| # | hypothesis | verdict | key statistic | script / result |
|---|---|---|---|---|
| 1 | Intraday sector momentum persists | **DEAD** | mean persistence r = −0.055, inside noise | `rotation_probe.py` |
| 2 | Persistence exists at *some* lookback/forward pair | **DEAD** | best \|r\| 0.3357 vs null p95 0.3613, p = 0.137 over a 5×5 grid | `rotation_sweep.py` |
| 3 | The effect is diffuse, so use a count test not a max test | **SURVIVES** | mean persistence, share-negative and breach-count all p = 0.00 (300 perms) | `rotation_diffuse.py` |
| 4 | …but is it real, or thin prices in small buckets? | **ARTIFACT** | corr(bucket size, r) = +0.475; reversal shrinks as buckets widen; vanishes on published indices | `rotation_bucketsize.py` |
| 5 | Reversal survives an execution gap | SURVIVES (moot — see #4) | p = 0.00 at gaps 0/1/2, collapse 0.067 | `rotation_bounce.py` |
| 6 | Rotation is driven by macro (crude/USDINR/gold/VIX) | **DEAD** | 18.2% of effect survives controls; mean residual r = 0.0004 | `rotation_causes.py` |
| 7 | Rotation tracks daily FII/DII flow | **UNDERPOWERED** | n = 35, noise band 0.331 | `rotation_daily.py` |
| 8 | Sector returns track FII index/stock positioning | **UNDERPOWERED** | 15 "stars" vs 2.2 expected, but no null run on the family | `rotation_fii.py` |

**Bottom line:** intraday sector rotation is a small-bucket pricing artifact. Do not rebuild it.

### Macro → index

| # | hypothesis | verdict | key statistic | script / result |
|---|---|---|---|---|
| 9 | USDINR / crude / VIX / own-return predict 11 sectors | **DEAD** | 18 survivors vs null median 15, p95 21 → p = 0.225 | `horserace.py`, `horserace_null.json` |
| 10 | …at least crude does, on a count test | **SURVIVES (weak)** | crude count 16 vs null median 10, p = 0.03; but max-\|t\| p = 0.35 | `maxt_null.py` |
| 11 | High/rising crude predicts Nifty weakness | **DEAD** | crude high+rising: next-1d p = 0.979. Top quintile next-1d +0.009% vs +0.043% baseline | `crude_analogue.py` |
| 12 | Crude beta is stable | **MEASURED** | full-sample β = +0.0064; 2026 β = −0.039. Sign flips by regime | `crude_priced.py` |
| 13 | SP500 × crude *interaction* (not additive) predicts | **SURVIVES** | quadrant spread 0.44, p = 0.00; t(interaction) = 2.79; R² 7.21→7.63 | `regime_test.py` |
| 14 | US indices lead Nifty IT next session | **SURVIVES (weak)** | NASDAQ→IT intraday r = 0.066, p = 0.0025, n = 2005. Gap r = 0.146 but untradable | `us_leads_it.py` |
| 15 | Breadth modifies the index signal | **DEAD** | bullish/broad vs bullish/narrow spread 0.132, p = 0.196; t(interaction) = 1.20 | `breadth_modifier.py` |
| 16 | Velocity/acceleration adds over VIX level | **SURVIVES (weak)** | R² 31.83 → 33.13; t(accel) = −3.48. Sign is *mean-reverting*, not continuation | `velocity_test.py` |
| 17 | Index closes high in range despite weak breadth = bullish absorption | **DEAD** | 161 days, next-day −0.019% vs +0.046% baseline, p = 0.79. Sign flips across thresholds | 2026-08-13, this register |
| 17b | …but strong close **with** strong breadth | **SURVIVES** | +0.069 to +0.078pp over baseline at every threshold, win 59–61%, n = 434–627 | same |

**Bottom line:** the daily macro→index regression was retired at R² = 0.036. Macro explains; it does not forecast. Only the SP500×crude interaction and a weak US→IT lead survive.

### FII positioning

| # | hypothesis | verdict | key statistic | script / result |
|---|---|---|---|---|
| 18 | FII sector positioning predicts sector returns | **UNDERPOWERED** | 132 tests, 11 hits, min detectable r = 0.179 | `fii_battery.py` |
| 19 | FII positioning works within regimes | **DEAD** | 5-day spread p = 0.0375 but interaction p = 0.23 over 400 perms | `fii_regimes.py` |
| 20 | …and it is not just the expiry cycle | **DEAD (confounded)** | corr(position, day-of-cycle) = −0.529; residualised p = 0.32–0.57 | `fii_confound.py` |
| 21 | FII derivatives activity (8 signals) predicts next day | **DEAD** | max-\|t\| null: close-close p = 0.202, intraday p = 0.220 | `fii_deriv.py` |
| 22 | …it predicts the overnight **gap** | **SURVIVES, UNTRADABLE** | p = 0.022; but the file publishes *after* the close, so close(D)→open(D+1) is unreachable. Every reachable target dead (p = 0.11–0.66) | `fii_gap.py` |
| 23 | FII positioning works in shock regimes | **DEAD** | 0 of 28 subgroup tests cleared p<0.05; chance ≈ 1.4 | `fii_shock.py` |
| 24 | FII net short **level** predicts returns | **DEAD** | raw level r = −0.50 at 20d looked strong, but non-overlapping n = 12 → p = 0.635; split-half −0.28 vs −0.90; z-scored fails everywhere | 2026-08-13 |
| 25 | FII "know" the market falls | **DEAD** | same-day r = +0.690, next-day r = +0.007. Largest short build was March 2026 — the month it bottomed. Max bearish across futures/puts/calls on the exact low | `fii_shock.py` + daily March analysis |
| 26 | FII are always net short index futures | **SURVIVES (fact)** | 245/245 days net short; range −87,170 to −279,467; median −181,339 | `participant_oi` |
| 27 | The short is a hedge of their cash book | **DEAD** | ₹39,518 cr ≈ 1% of ~₹40 lakh cr FII-held Nifty 50 equity | 2026-08-13 |
| 28 | Position size tracks the futures premium (carry) | **SUGGESTIVE, n = 5** | carry 30.4% (May) → 3.4% (Jul); Pearson −0.616, Spearman −0.300 on 5 months. April contradicts | 2026-08-13 |

**Bottom line:** FII derivatives positioning has no predictive content at any horizon, in any regime, in any specification tested. It is best explained as the counterparty print of a structurally long retail book (retail 3.0 longs per short; FII hold 67.8% of all short OI), sized against the carry.

### Theta / premium selling

| # | hypothesis | verdict | key statistic | script / result |
|---|---|---|---|---|
| 29 | A variance risk premium exists | **MEASURED** | realised/implied = 0.673 across **all** VIX buckets — the edge does not concentrate | `theta_study.py`, `conditional_vrp.py` |
| 30 | VRP is wider after broad vs concentrated declines | **DEAD** | broad − concentrated = 0.044, p = 0.380 | `theta_study.py` |
| 31 | The tail is manageable | **MEASURED** | ratio > 1 on 20.8% of days; worst 1% = −70.96pp against +984.59pp gross wins | `theta_tail.py` |
| 32 | Path risk ≈ terminal risk | **DEAD** | median MAE 2.35% vs terminal 1.45%; breach 75.7% vs 44.7%. Concentrated vs broad p = 0.810 | `path_risk.py` |
| 33 | Overnight is the dominant risk | **MEASURED — regime-dependent** | full sample gap share 40.6%; **2026 YTD 71.8%**; 2023 28.3%. See correction C5 | `overnight_test.py` |
| 34 | Buying wings is cheap protection | **DEAD** | call wing keeps 78% of theta, cuts ES only 7%, and makes the worst case **worse** (−19.65 vs −17.17) | `wings_real.py`, `wings_pnl.py` |
| 35 | The wing premium is a fat-tail premium | **ARTIFACT** | it is put skew: put IV 14.89% at −2.6% OTM vs flat call side 12.56–12.91% | `smile.py` |
| 36 | Wider spreads pay better | **MEASURED** | total P&L rises 50→300 wide (645→784), then falls at 400. Worst case improves monotonically | `width_sweep.py` |
| 37 | Active management beats static | **MEASURED** | STATIC total 689.6, worst −269.1. H50-R+resell total 591.1 but **worst +9.7** | `mgmt_sweep.py`, `roll_backtest.py` |
| 38 | Rolling in the direction of spot helps | **MEASURED** | up-rolls +6.4 pts/roll, down-rolls −9.2 (1.44× asymmetry). A down-roll is a loss-crystallisation event by construction | `spread_roll.py`, `bullput_roll.py` |
| 39 | Regime conditioners pick good bull-put entries | **RETRACTED** | down-days-in-5 spread 0.0993 vs null median 0.0646, p95 0.1357 → **p = 0.194**. 3-day drift p = 0.335, last-day move p = 0.302 | `persistence.py` |
| 40 | Bear call is the bearish mirror of the bull put | **DEAD** | bear call −0.0346 mean / 63% win unconditionally, and **−0.0433 in its own bearish regime** vs bull put +0.0219 in its own. Switching (−0.0066) worse than always-bull-put (+0.0011) | `mirror.py` |
| 41 | A stop-loss / cooldown improves the book | **MEASURED** | see result files; cooldown keyed on observable inputs, not realised outputs | `stoploss.py`, `cooldown_chain.py`, `exit_early.py` |
| 42 | Bounce after MAE breach | **SURVIVES (weak)** | mae1 t = −3.70, ΔR² 0.392; mae2 t = −3.99 | `bounce_test.py` |
| 43 | An earnings print should be graded against **implied** EPS growth, not zero | **PRIOR — n = 2, NOT VALIDATED** | Apollo: implied 59.2% vs reported 38.4% → gap −20.8pp, stock −3.49%. Grasim: implied 101.0% vs 51.0% → −50.0pp, −3.79%. Both signs correct, but two observations is a demonstration, not evidence | `backend/quant/news_fundamentals.py` |
| 44 | Valuation state (P/E percentile, implied EPS growth, target upside) explains the cross-section of daily moves | **DEAD (one session, underpowered)** | 13 Aug close, n=49: corr with day return −0.110 (P/E pctile), +0.115 (implied growth), −0.057 (target upside). P/E sign flips to +0.299 over 3 sessions. Richest mover (LT, 75th pctile) rose most; largest drag (ICICI, 25th pctile) was cheap | 2026-08-13, `news_fundamentals.py` |
| 45 | The futures BASIS predicts the index | **DEAD (underpowered)** | NIFTY_FUT_1 vs spot, n=67 daily: r = +0.103 (1d, p=0.43), −0.114 (5d, p=0.57), −0.453 (10d, p=0.11). Terciles non-monotonic (cheap +0.158%, mid +0.508%, rich −0.141% fwd 5d). **Minimum detectable \|r\| at n=67 is 0.239** — this is 'cannot see', not 'nothing there' | 2026-08-13 |
| 46 | The closing auction (CAS, from 2026-08-03) has a directional bias | **PRIOR — 9/9 positive, n far too small** | jumps +201.0, +151.5, +54.5, +8.0, +13.7, +23.6, +21.5, +74.2, +42.5. Mean +65.6, median +42.5. Sign test p=0.002, but 9 sessions of a brand-new mechanism is a settling-in period, not a stable regime | 2026-08-13 |

| 47 | A weight-ordered quality screen (net-profit consistency ≥70%, 5y profit CAGR ≥10%, implied forward growth ≥15%) beats the index | **NOT ESTABLISHED** | Corrected twice, see C21. Point-in-time, buying 92 days after each cutoff so results are traded only after publication: 2021 cutoff gives **+5.2pp total over 5.1y = +0.7pp annualised**. Start-date sweep over four cutoffs (2020-2023): excess CAGR **+0.1 / +0.7 / +3.5 / +4.6pp**, median +3.5, sign positive 4/4 — but the starts are NOT independent (overlapping periods, overlapping holdings) and the magnitude spans 45x. The look-ahead version of the same screen returns +85.6pp. Consistent sign, unstable magnitude, no independent replication: not a finding | 2026-08-14, `data_agent/fundamentals/quality_growth.py` |

| 48 | Aggregate Nifty-50 earnings can be measured rather than assumed | **SURVIVES (fact)** | Balanced panel of **47 names, 96.9% of index weight**, complete FY2018-FY2026, all 31-March year-ends. Aggregate net profit ₹271,829 cr → ₹844,074 cr (+210%). YoY: +18.0, −12.1, +24.4, +54.3, +8.0, +19.9, +12.7, +6.9. Median **15.3%**; excluding the FY21/FY22 COVID base effects median **10.3%**, mean **8.9%**. Last three years decelerate monotonically | 2026-08-14, `backend/quant/index_valuation.py` |
| 49 | The index's own trailing P/E can be reconstructed from that panel | **MEASURED, with a stated anchor** | 2,001 daily observations 2018-07 → 2026-08. Median **25.4×** (24.1× ex-COVID), range 16.8-39.8×. Today **20.39× = 3rd percentile** (4th ex-COVID). The LEVEL is anchored to today's bottom-up weighted P/E, so today matching the index card is true by construction and is not a validation; only the SHAPE is measured. Three biases all flatter today's valuation: constant constituents, the COVID denominator, and assumed mean reversion | 2026-08-14, `index_valuation.py` |
| 50 | Axis's Dec-2026 targets are a re-rating call | **DEAD — they are the opposite** | Mapped into our units, the Axis exit multiples are **20.1-21.0×, the 2nd-5th percentile** of the index's own 2018-2026 P/E. Their bull case assumes the multiple stays near its eight-year low; all of their upside is earnings. Their 12-14% FY27 growth is the ~50th percentile of our eight observed years but an acceleration from the last observed 6.9% | 2026-08-14, `nifty_outlook.py` |

| 51 | The annual earnings figure describes the trend | **MISLEADING — the exit rate is far lower** | Aggregate quarterly PAT growth, YoY, balanced 47-name panel: **+18.8 → +12.6 → +9.6 → +4.4 → +2.5%**. On the consistent 37-name panel that has reported Q1 FY27 (82.6% of weight): **+12.4 → +7.2 → +7.9 → +3.5 → +3.7%**. FY26 annual growth of +6.9% is the average of a year that decelerated through itself; the exit rate is **~3.7%**. Aggregate operating margin **27.5%, −1.2pp YoY** — a second measured channel pointing the same way | 2026-08-14, `backend/quant/earnings_acceleration.py` |

| 52 | The outlook methodology (EPS × multiple) beats a no-change null | **DEAD at 6M and 1Y** | Walk-forward from 25 quarterly as-of dates (2020-07 →), 92-day publication lag, k cancels out of every ratio so the valuation anchor leaks nothing. MAE vs the null: **6M** run-rate −3.7pp WORSE, reference-median −8.6pp worse; **1Y** run-rate −7.5pp worse, reference-median −5.4pp worse. Only a pure price-based drift method beats the null, and barely (+0.3pp at 6M, +1.7pp at 1Y). 2Y shows +12.4pp for drift on **2 independent windows** — not evidence. NOTE the null itself is biased −7.9/−15.8/−29.9pp in this sample, so 'beating the null' here largely means 'being bullish enough' | 2026-08-14, `backend/quant/outlook_backtest.py` |
| 53 | A below-median index multiple reverts upward | **DEAD — it kept falling** | Of as-of dates with the multiple BELOW its own median, it ROSE over the next 6M in **43%** (n=14), 1Y in **42%** (n=12), 2Y in **25%** (n=8). Median realised multiple change 0.936× / 0.910× / 0.789× against the 1.03× the reference rows assume. Reconciles with the tercile table's +22.3% cheap-start 1Y return: those windows made money **through earnings while the multiple compressed further**, not through re-rating. Directly contradicts the mean-reversion assumption embedded in every P/E-percentile scenario | 2026-08-14, `outlook_backtest.py` |

| 54 | Crude is the biggest shock to Nifty EARNINGS | **NOT SUPPORTED for the DIRECT channel only** | corr(fiscal-year WTI change, aggregate panel profit growth) = **+0.731, n=8, min detectable |r| = 0.74** → NOT DETECTABLE, and the sign is POSITIVE because ~25% of index profit is crude/commodity producers. Profit-share decomposition: other non-financial **41.3%** (loses on crude), financials **33.9%**, energy **14.5%**, metals **10.3%** (both gain). Arithmetic sensitivity: a 10pp hit to the consumer cohort with a 10pp commodity gain nets **−1.6pp** of index earnings growth; a 15pp hit with a 10pp gain nets −3.7pp. Bounded for the DIRECT channel only. The macro/FX/policy and demand channels are NOT measured here and H59 shows the valuation channel alone cost 13.4% in 2026 with earnings RISING. Because financials supply the largest earnings-growth contribution (+5.0pp) and have no direct crude line, crude's real exposure here is second-order: it can prevent the acceleration the 10-13% case needs rather than cause a decline. Profit share is not a crude beta — Reliance is 9.2% of the index and its refining margin can widen when crude falls. The base is only ~3.7%, so even a bounded shock takes index earnings growth to zero | 2026-08-14, `backend/quant/crude_earnings.py` |
| 55 | The earnings slowdown is a crude story | **DEAD — it is a composition story** | Quarterly YoY on FIXED member sets: financials (33.9% of profit) **+1.7 → +10.0 → +16.2%, accelerating**; metals **+84.8 → +5.1%**, energy **+46.5 → +3.5%** — the commodity tailwind that carried FY26 is collapsing; other non-financial **+3.7 → −11.2 → +4.5%**, choppy and flat. The index exit rate of ~3.7% is these four legs netting, with crude at $81 — i.e. the current earnings problem is occurring while crude is CHEAP | 2026-08-14, `crude_earnings.py` |

| 56 | Contribution, not share, is the right lens on index earnings | **SURVIVES (fact)** | contribution_pp = (profit_t − profit_{t−1}) / total_{t−1}; the four cohorts sum exactly to aggregate growth. Quarterly build, fixed 37-name panel: **Jun-25** energy +8.1 · **Dec-25** metals +5.5 · **Mar-26** metals +7.8 against other non-financial −5.7 · **Jun-26** financials +5.0, energy −3.7, other +2.0, metals +0.4 = **+3.7pp**. The engine has rotated twice in five quarters and is now financials alone. To reach 13% with every other cohort repeating its latest quarter, financials would need **42% growth**; to reach 10%, **33%** | 2026-08-14, `backend/quant/crude_earnings.py` |

| 57 | The 9.3pp bridge to 13% needs a NEW earnings engine | **DEAD — the largest single piece is a base effect reversing** | Q1 FY27 sector contributions (fixed 37-name panel): Financials **+5.01pp**, IT +1.57, Autos **+1.40**, Metals +0.44, Power +0.43, Construction +0.31, Consumer Durables **+0.27**, Cement +0.23; against FMCG −0.51, Healthcare −0.53, Services −1.29, **Oil & Gas −3.69**. Total +3.73pp. The Oil & Gas drag is ONE name (16.5% of panel profit) down 22.4% YoY because the year-ago quarter contained a one-time Asian Paints stake-sale gain — a base effect that self-corrects, worth ~**3.7pp of the 9.3pp gap** with no operating improvement anywhere | 2026-08-14, `crude_earnings.py` sector split; base effect confirmed via press |
| 58 | Luxury/discretionary and EV-led autos can carry index earnings | **NOT ON THIS ARITHMETIC** | Consumer Durables is **0.7% of panel profit** — growing 40.0% YoY it contributes **+0.27pp**; doubling profit would add 0.7pp. Automobile is 6.9% of profit, +20.4% YoY, **+1.40pp** (M&M +33.6%, Bajaj Auto +45.9%, Eicher +21.3%, Maruti −9.1%). Both cohorts growing strongly and both structurally too small to move a 9.3pp gap: at a combined +50% YoY they add ~+3.8pp. Real acceleration, wrong order of magnitude | 2026-08-14 |

| 59 | The 2026 drawdown was an earnings event | **DEAD — it was 100% multiple** | 2-Jan 26,329 at **23.56×** → 30-Mar 22,331 at **19.98×**. NO annual result was published in that window (FY25 base throughout), so the entire **−15.2%** was de-rating with the earnings denominator literally unchanged. Concurrent: WTI **63 → 100**, VIX **9.4 → 27.9**, USDINR **90.00 → 94.36**. Year to date: price −7.5% = earnings **+6.9%** × multiple **−13.4%**. Had January's multiple held, today's index on FY26 earnings would be **28,150** against an actual 24,366. The cleanest natural experiment in the sample for the VALUATION channel, precisely because no earnings data arrived to contaminate it | 2026-08-14 |

| 60 | Recent earnings growth predicts next year's | **NOT DETECTABLE** | Annual AR(1) on aggregate panel profit growth: **r = −0.121, n = 7, min detectable |r| ≈ 0.80**. Growth series 18.0, −12.1, 24.4, 54.3, 8.0, 19.9, 12.7, 6.9. So 'the run-rate continues' is an assumption with no observed support — the same conclusion H52 reached from the other end, where the run-rate method lost to a no-change null. Equally, nothing here supports the historical MEAN either; at n=7 no lookback window is distinguishable from any other, so no weighting is applied | 2026-08-14, `index_valuation.regime_layers()` |
| 61 | 'What P/E does this earnings regime deserve?' can be answered from history | **NOT ON TRAILING MULTIPLES — the question is near-circular** | Mean multiple paid on each fiscal year's earnings: FY20 growth **−12.1%** → **33.50×**; FY21 +24.4% → 34.24×; FY22 **+54.3%** → **23.21×**; FY26 +6.9% → 20.30×. corr(growth, multiple paid) = **−0.279, n = 8, min detectable 0.74** → not detectable, and the sign is negative because a TRAILING multiple moves mechanically inverse to its own denominator. Conditioning trailing P/E on trailing growth measures the arithmetic, not the market's preference; it needs forward EPS, and this repo holds ONE forward snapshot. Note 2,124 daily P/E observations but only **8 independent regime-years** | 2026-08-14 |

| 62 | "FII are selling" is a directional signal | **DEAD — net is 0.22% of gross** | Cash market, 38 sessions 18-Jun→13-Aug-2026: FII gross buy **₹591,528 cr**, gross sell **₹594,189 cr**, net **−₹2,661 cr** — **0.22% of ₹11.86 lakh cr of gross turnover**. Daily net/gross: median **3.4%**, above 10% on only 5 of 38 days. DII by contrast run net at **5.13%** of gross (+₹70,345 cr) — 23× more directional. Every headline quoting an FII net number is quoting a rounding residual on a two-way flow. This is WHY the FII-predicts-returns battery (H18-H25) failed: there is almost no directional content in the number | 2026-08-14, `fii_dii_flows` gross columns |
| 63 | The FII index-futures short is a bearish view | **RECONSIDER — it pairs with a stock-futures LONG** | 245 sessions Aug-2025→Aug-2026. Index futures: net/gross **75.2%** (median), net **always negative**, range −87,170 to −279,467 — overwhelmingly one-sided. Stock futures: net **always POSITIVE**, +496,849 to +1,603,502, net/gross +20.2%. So the book is **long single stocks, short the index** — a long-alpha / short-beta structure, i.e. relative value, not a directional bearish bet. Reconciles H26 (always net short index) with H27 (the short is only ~1% of their cash equity, so not a cash hedge either). CAVEAT: contract counts, not notional — the two legs cannot be netted without lot sizes and prices | 2026-08-14, `participant_oi` |

**Bottom line:** the VRP is real and flat across VIX. Nothing tested improves entry timing. Management (rolling, harvesting) improves the *worst case* far more than the mean.

---

## 2. Corrections and retractions

Every one of these was reported as a finding before being found wrong. They are
listed because the failure modes repeat.

| # | claim as first reported | what was actually true | cause |
|---|---|---|---|
| C1 | Crude β (2018–24) = +0.0371 | **+0.0088** | ffill across mismatched US/India holiday calendars manufactured fake zero-return days |
| C2 | VIX model R² = 33.01%, t = −26.6 | **R² = 7.21%** | same-day India VIX in a next-day model. VIX is computed from index options — mechanically inverse to same-day Nifty |
| C3 | Breadth ranged 0–1.7% | 0–100% | base taken from `iloc[0]`, NaN for late-listing symbols. Fixed with `bfill().iloc[0]`. Third instance of this bug class |
| C4 | All 106 gamma-break events "unconfirmed" | mixed | confirmation condition unsigned by break direction; fixed with `(breadth − 50) * dir` |
| C5 | "56% of path movement arrives overnight" | **39% full sample / 71.8% in 2026** | summed \|gaps\| over 6-day windows instead of decomposing squared returns. Also: the covariance term was dropped, so shares summed to 117% — gap and intraday are negatively correlated (−0.145) |
| C6 | Crude is "$9 stale" vs Brent | ~$3.6 | the database carries **WTI (CL=F), not Brent**. See `data_agent/quality/split_mixed_symbols.py` |
| C7 | Wings mispriced vs σ = VIX | no mispricing | India VIX is a model-free variance-swap-style aggregate (1/K² over the OTM strip), **not** a Black-Scholes single-strike IV. Redone by inverting BS per strike on 20,990 LTPs |
| C8 | +28% wing gap = fat-tail premium | **put skew** | see #35 |
| C9 | Buy call wings, they're "free" | they don't protect | see #34 |
| C10 | "Persistence discriminates ~2× better than magnitude" | fails its null at p = 0.194 | claimed before scoring. See #39 |
| C11 | "No anchor exists for the FII futures level" | it was in `.state/flows_cash_cache.json` as `fii_fut_net` | searched the DB and repo root, not `.state/` |
| C12 | 1d close ≠ 1m close is an alignment bug | **NSE methodology** | the official daily close is the last-half-hour VWAP; 1m bars are last-traded price. The two coincide exactly from **2026-08-03** onward, when the daily series began deriving from the 1m series |
| C13 | Daily-bar basis (+117 pts) disagrees with matched-minute (+57) → method bug | **sample period** | the two agree within ~10 pts on identical sessions (r = +0.37). Apr–Jun genuinely had a wider basis |
| C14 | corr(FII futures change, next-month return) = **+0.811** | **−0.515** | `groupby(...).agg('last')` on a frame queried without `ORDER BY` — "last" picked an arbitrary row per month |
| C15 | Drawdown futures P&L = +₹7,473 cr | **+₹5,180 cr** | anchor-free estimate assumed C = 0; actual 2-Jan position was −130,246 |
| C16 | `participant_oi` "has never been collected" | the table existed with 0 rows; backfilled 2026-08-13 | checked table existence, not row count |
| C17 | Portfolio CAGRs of 16.9% (look-ahead book) and 10.0% (walk-forward) | **20.8% and 11.3%** | averaged the per-holding CAGRs instead of compounding the portfolio's own total return. Mean-of-CAGRs is not the CAGR of the mean: +175.7% over 5.37y is 20.8%, not 16.9%. Index CAGR (9.6%) was unaffected, which is why the error survived a sanity check |
| C18 | Annual EPS series were sound because they came from Postgres | **Q1 FY27 sat inside every annual series** | `eps_cagr_backfill.py` had no `time_period = 'yearly'` filter, so a quarter landed in the annual query. TCS `eps_end` read ₹32.70 (one quarter) against ₹136.01 (FY26); every IT major showed a 3y CAGR of −32% to −34% |
| C19 | Stored EPS is a single well-defined series | **wrong by multiples for banks** | `_EPS_LABELS` matched nine variants ('eps', 'basic eps', 'diluted eps', …) with no consistent winner, so different years came from different definitions: HDFC Bank 2.1×, Kotak 6.8×, Bajaj Finance 10.1×. Fixed by DERIVING EPS as net profit ÷ adjusted shares and never reading a stored EPS label |
| C20 | A SQL comment is inert | **`%` in a comment silently killed every query** | the comment read '−32%.'; psycopg3 parsed `%` as a parameter placeholder and the failure was swallowed by `except Exception: continue`. Found by the user's other agent |
| C21 | Quality-screen walk-forward excess = **+14.2pp** | **+5.2pp** (and +0.7pp annualised) | the screen read FY2021 annual results and bought on 1-Apr-2021. Indian March-year-end annuals are published in May-June, so the portfolio traded on numbers that did not exist for another 2-3 months. Moving the buy to 1-Jul cut the excess by two thirds. Found by self-review, not by a test |
| C22 | "Point-in-time" meant free of look-ahead | **it does not** | `delivery_history.json` is built from TODAY'S Screener exports, which carry restated history (Ind-AS transitions, reclassified consolidations, merger restatements). Every walk-forward figure in this repo built on it reads today's VIEW of the past. No point-in-time fundamentals archive exists here, so this is disclosed, not fixed — it makes H47 an upper bound |
| C23 | A single start date was enough to report an excess | **the start date dominated the result** | shifting the buy one quarter moved the 2021 excess ~10pp, larger than the excess itself. Any backtest with one entry date now reports a start-date sweep alongside it |
| C24 | Cohort quarterly paths recomputed eligible members each quarter | **fixed member set per path** | coverage changed underneath the series, so a cohort growing because a large member started reporting read as a cohort growing. Caught in review before shipping; the corrected 'other non-financial' path reads +6.3 → +5.7 → +3.7 → −11.2 → +4.5 against a mixed-panel +8.8 → +10.5 → −4.6 → −11.4 → +4.5 |
| C25 | "The Nifty low (30-Mar) PRECEDED the WTI peak (7-Apr) by 8 days, so the sequencing is backwards for a crude story" | **the peak date is not the shock date** | WTI ran 63 → 100 THROUGH March, exactly while the index fell 25,179 → 22,820. Equities bottoming a week before a commodity tops out is normal, not evidence against transmission. Using a single peak date to time a shock that took two months to develop was the error |
| C26 | Aggregate operating margin reported as 27.5%/28.6% across all 47 names, with a caveat stating that banks were excluded because they report no meaningful sales line | **the caveat was false — the banks were in the panel the entire time**, and the margin is now computed on non-financials only (37 names) | a bank's Screener `sales` row is INTEREST EARNED and its `operating profit` does not net interest expense, so the ratio is not a margin: SBI read 70.8%, HDFC Bank 68.8%, ICICI 69.0%, Axis 58.6%, Kotak 58.5%. Found by cross-checking Trendlyne's OPM % TTM screener for the Nifty 50, where 39 of 47 names agreed to about a point (TCS 27.1 vs 26.7, Infosys 23.7 vs 23.9, HUL 23.3 vs 23.0, Sun Pharma 30.3 vs 30.1) and the five large banks diverged 15.6-45.4pp. Excluding financials moves the aggregate from 28.6% to **20.2% TTM / 18.8% in the March-2026 quarter, −1.5pp YoY**. The wrong number was worse than a wrong number: a false caveat told the reader the check had already been done. Consequence stated rather than papered over — the margin channel now covers ~77% of panel revenue and is BLIND to the cohort supplying the largest share of index earnings growth |
| C27 | Today's reconstructed index P/E is at the **3rd percentile** of its own history | **~11th percentile (full sample) / ~13th (trailing 5y)** once the history is put on a comparable footing | second-source check, 16-Aug-2026. TODAY is fine — three independent NSE-derived publishers read 20.56 / 20.56 / 20.6 against our 20.39 at spot 24,366, inside 0.9%; implied index EPS 1,185 against our 1,195, also 0.8%. The HISTORY is not: indexpe.in publishes a trailing-5y median of 22.06 and ours is 23.88, **8.2% rich**. The asymmetry is mechanical — NSE's EPS rolls four times a year as each quarter's TTM lands, ours steps ONCE 92 days after 31-March because the panel is built from annuals, so our denominator is stale-LOW for most of every year and the printed P/E is too high. With staleness ~uniform on [0,1] year and growth g, expected overstatement is g/ln(1+g) = **1.063 at g=12.9%, against 1.082 observed** — the mechanism accounts for ~3/4 of the gap, the rest being 47-of-50 coverage and PAT definition. It bites because the bias sits in the DENOMINATOR of the percentile: today is anchored to a true bottom-up value, the history it is ranked against is inflated, so 'cheap' was overstated. Still a low reading; not an extreme one. `pe_series()` now returns a `cross_check` block carrying both numbers |
| C28 | Aggregate FY26 panel profit stated as **₹844,074 cr** without an external check | **kept, with the offset measured and its scope stated** | Chola Securities' Q4FY26 review puts Nifty-50 FY26 PAT at **₹10,08,780 cr** (FY25 ₹8,74,313 cr, +15.38% reported, **+3.72% ex-exceptional**); Angel One reports **+9.1%**. Our level is 16.3% lower and the three missing names (JIOFIN, NESTLEIND, TATAMOTORS) are only **3.05% of index weight**, so the gap is DEFINITIONAL — reported PAT including exceptionals versus the Screener line we sum — not coverage. It does not propagate: the projection uses `(1+g)^T × (exit_PE/PE_asof)`, so a stable level offset cancels exactly the way the P/E anchor constant k does. What must be right is the GROWTH rate, and ours (**+6.9% FY26**) sits inside the published range and nearest the ex-exceptional basis. The level is a panel aggregate, not a claim about the index's reported profit, and should not be quoted as one |
| C29 | Life insurers and financial holding companies were swept into the aggregate OPERATING-MARGIN cohort because Screener renders them on the generic template | **excluded by RULE — a Financial Services name Screener does not give the lender template to has no comparable margin** | their revenue line is premium or dividend income, not operating revenue: SBI Life read **1.6%**, HDFC Life **1.5%**, against Reliance 15.4% and TCS 25.7%, while JIOFIN read **69.7%** as a holding company and BAJAJFINSV **40.8%** by consolidating a lender's spread with two insurers' premium. The level effect was only **0.21pp** (18.97% -> 18.76%) which is exactly why it nearly shipped — but the level is not the danger, the SENSITIVITY is. Controlled counterfactual, every company's profit held constant, growing only the excluded cohort's top line: the aggregate falls **18.97% -> 18.10%** at +50%, purely through denominator growth, while the measured-only series cannot move at all. A channel built to detect margin deterioration would have been reading its own artifact — time-series contamination, not cross-sectional bias. Second, independent reason (user's): premium income is policies x average premium, so it can rise on price, mix or renewals with policy count FALLING — 900,000 at Rs 50,000 beats 1,000,000 at Rs 40,000 — so the denominator is not interpretable as revenue growth either. Their PROFIT stays in the earnings panel: same company, in for one metric, out for another. This is metric exclusion, not missing data, and the two are now counted separately (`not_applicable` 2.8% vs `data_unavailable` 0.0%) because collapsing them hides which gaps are closeable |
| C30 | Lender cohort reported as **+7.0pp YoY** financing margin (8.8% -> 15.8%) | **+1.9pp on a four-quarter TTM basis**; the quarterly series is not trend-readable | the parse was right — our recomputation matches Screener's own published Financing Margin % to within 0.5pp — but the series lurches because Screener's `Expenses` for a lender absorbs provisions, which are lumpy: Axis 8.0 -> 1.0 -> 1.0 -> 7.0 -> 1.0 -> 8.0, HDFC Bank 15.0 -> **-1.0** -> 13.0. The +7.0pp compared a TROUGH quarter with a PEAK quarter. Measured: QoQ stdev **2.30pp** for lenders against **0.89pp** for non-financials, max swing 4.4pp; on TTM it falls to **0.82pp**. TTM is now the headline for both cohorts and the volatility diagnostic ships inside the artifact, so the choice is justified by data rather than asserted. Caught in review before the number reached the tracker |
| C31 | Financing margin adopted as the lender cohort's profitability measure, verified only against Screener's own published `Financing Margin %` row | **arithmetic confirmed against two banks' filed results — and the check revealed the metric omits roughly HALF of bank profit** | matching our recomputation to Screener's own row proved our parsing, not Screener's definition: checking a sum by adding the same column twice, which is exactly how C26 survived for weeks. Reconciled instead against HDFC Bank and SBI Q1FY27 filings. Everything ties: HDFC Revenue 79,363 vs Interest Earned 79,362.78, Interest 45,829 vs Interest Expended 45,826.83, Net Profit 19,060 vs 19,059.72; SBI Net Profit, Other Income (15,923) and Gross NPA % (1.47) match to the rupee and basis point, and Revenue - Interest = **46,992 = the filed NII exactly**. Two findings came out of it. FIRST, implied `Expenses` of 21,247 less HDFC's filed provisions of 3,060 leaves 18,187 — its actual quarterly opex — so **Screener buries provisions inside the cost line**, which CONFIRMS by arithmetic the lumpiness that C30 had only inferred from the volatility pattern. SECOND, and material: **Financing Profit excludes Other Income entirely**, from numerator and denominator both. Other Income is 12,822 against HDFC's Financing Profit of 12,287 (1.04x) and 15,923 against SBI's 12,560 (1.27x) — so **51% and 56% of pre-tax profit respectively sit OUTSIDE the metric**. FinProfit + OtherIncome - Depn reproduces reported PBT to within 1 cr at both banks, confirming the structure. Financing margin therefore measures the lending spread net of costs and is NOT a proxy for bank profitability; fee, treasury and distribution income are invisible to it, and SBI's fee income grew 20.83% while treasury fell in the same quarter. The number stays; what it is allowed to CLAIM is narrowed, and the caveat now ships inside the artifact rather than living in a conversation — which was C26's actual failure |
| C32 | Aggregate panel profit treated as comparable to published street figures, leaving 84% of the C28 gap 'unexplained' | **two different line items, and ours is the correct one for an index EPS** — the gap is a definition, not an error | single-company reconciliation against filed results. **JSW Steel is the decisive witness because both definitions appear for one company**: its FY26 filed net profit *attributable to owners* is **22,316 cr — exactly our export figure** — while the page source reads 25,508 including minority interest. Confirmed the other way on TCS (page 49,454 = street 49,454) and Reliance (page 95,754 = street 95,754, +17.8% = street +17.8%), where our export reads 80,775 because external holders of Jio Platforms and Reliance Retail own a large minority. Names WITHOUT minority interest agree exactly across sources — HDFCBANK 10.9% vs 10.9%, INFY 10.2% vs 10.2% — so the divergence is concentrated entirely in holding structures (Reliance, JSW, Grasim, Bajaj Finserv, ONGC, Bharti). **Ours stays**: index EPS must be earnings attributable to the constituents' own shareholders, which is why our index EPS of 1,195 matches NSE's implied 1,185 within 0.8% *while* the aggregate sits 16% below the street. Both true at once, not in tension. The C28 residual is therefore RESOLVED, not outstanding. The fix is labelling, not arithmetic: the artifact must state that the aggregate is attributable-to-owners and deliberately NOT comparable to newspaper headlines — an implicit choice that someone will later check against a newspaper is C26's exact shape |
| C33 | FY26 index growth of **+6.92%** quoted as a clean measurement | **+6.68% ex-one-off — and the headline is close to right only BY LUCK** | two large corporate actions sit inside it, pushing opposite ways and very nearly cancelling. JSW Steel booked an **18,051 cr gain** on losing control of BPSL's steel business (FY26; filed normalised PAT 8,698 vs reported 22,316), inflating FY26. ITC booked a **15,145 cr gain** on the ITC Hotels demerger (FY25), inflating the BASE and so depressing FY26 growth. Strip only JSW and FY26 growth is **+4.63%**; strip only ITC and it is **+9.01%**; strip both and it is +6.68% against +6.92% reported. **A 4.4pp range depending on which distortion you correct** — so the reported figure is not robust, it is coincidentally central. This matters because the whole hypothesis is framed as 'can the current rate reach 12-14%', and two names alone move the starting point by more than 4pp. Related: JSWSTEEL contributes +2.38pp and ITC -1.78pp of the reported +6.92%, so **two of 47 names account for more than half the index growth number in absolute terms**. Found by spot-checking individual companies against filed results rather than trusting the aggregate |
| C34 | Q1 FY27 exit rate **+3.73%**, path read as **decelerating**, hypothesis framed as 'can 3.7% become 12-14%' | **+7.12% on 47 names, and the path TROUGHED AND TURNED UP** | three compounding faults, each individually small enough to survive review. (1) STALE COVERAGE: the tracker read `delivery_history.json`, frozen at the 37 of 47 names that had reported when the workbooks were pulled on 15-Aug — and `download_screener.py` skipped every cached workbook while printing 'All downloads complete', so a deliberate refresh changed nothing. (2) DEFINITION: the page source folds in minority interest, worth **-5,347 cr on ONGC alone**, which consolidates 100% of HPCL's and MRPL's OMC losses while owning ~55% of them; that single name made oil & gas look like a -29.7% drag when ET NOW showed the sector roughly at the index average. (3) ONE-OFF: Reliance's Jun-2025 base carries the 8,924 cr Asian Paints gain, worth **-4.4pp** on the reported rate. FIXED by deriving attributable profit as **EPS x shares** — EPS being attributable-profit-per-share by definition — from the page scrape, giving the export's line item with the page's coverage; cross-checked on 470 observations, **96% within 3%**, exact on Reliance, ONGC, TCS, HDFC Bank and Bajaj Finserv, with the residual being share-count timing (per-period share counts, not the latest, after Zomato's Blinkit issuance skewed 2024). **The corrected path reads +18.8 -> +13.1 -> +9.7 -> +4.5 -> +2.5 -> +7.1**, against the stale panel's +12.4 -> +7.2 -> +7.9 -> +3.5 -> +3.7. Ex the Reliance one-off it is **+11.84%**. The `decelerating` flag tested only the balanced path and would have kept printing after the freshest quarter turned; it is now paired with an explicit `turned_up`, and the tracker prints WHICH SOURCE it used so a stale-coverage repeat is visible. **This is the correction that changed a conclusion rather than a number** — the framing 'can 3.7% become 13%' carried its own answer; '+7.1% reported, +11.8% underlying' does not. Found by the user asking whether other companies had Reliance-style one-offs |

**The pattern in C1, C3, C5, C14:** a missing or misaligned value silently replaced
by a plausible one. Each was invisible until an arithmetic identity failed to close.
When a decomposition does not sum to 100%, or a reconstruction does not match a
known level, that is the bug announcing itself — do not round it away.
| C35 | The Quality Growth screen's **5-year net-profit CAGR** gate reported as a measure of delivered growth | **it also measures where the starting year sat in a cycle, and on financials that dominates** | CAGR is a two-point statistic: it inherits both endpoints. The 5y window starts at FY2021, and for lenders FY2019-21 is the bottom of the NPA cycle, so the gate rewards recovery off a trough as if it were compounding. ICICIBANK reads a 5y CAGR of **47.1%** against a **25.4%** median annual growth rate — a 21.7pp gap that is entirely base effect. The same mechanism runs the other way through one-offs: Reliance clears the 10% gate at **10.46%**, and at **7.90%** ex the 8,924 cr Asian Paints gain it does not clear it at all, so one corporate action decides one of fifteen selections (C33/O14). FIXED by publishing **median annual growth** beside the CAGR — median is endpoint-independent, so the GAP between the two is itself the base-effect measure, and a large gap is now visible in the table rather than hidden inside a single number. The gate still uses CAGR; what changed is that a CAGR earned off a trough no longer looks identical to one earned by compounding. Consequence for the register: **14 of 15 selected names are priced above their own delivered median**, and 61.5% of selected weight is Financial Services — a sector concentration that the trough-recovery arithmetic partly manufactured. Found by asking why the screen held zero IT names while pricing TCS for 18% growth |
| C36 | `run_expectation_snapshot.sh` reported **"OK snapshots 2 -> 4"** and the weekly capture channel treated as working | **one real capture and one byte-identical duplicate; the wrapper logged OK twice for the same second** | the wrapper's only proof of success was that the snapshot COUNT went up — which is exactly what a duplicate write does. Two instances started at 09:00:01, each read the file before the other wrote, and both computed AFTER=4: a classic read-modify-write race. Snapshots #3 and #4 shared a `captured_at` to the second and an identical md5 over all 50 rows. This matters beyond one wasted row because `MIN_SNAPSHOTS_FOR_REVISIONS = 3` gates the revisions channel on that count, so the duplicate would have OPENED the highest-value evidence channel on an observation that contains no information — the failure mode C34 was about (a number that reads fine and is measured on nothing), arriving through the counter instead of through coverage. Fixed in three places because the bug had three halves: a **lock** in the wrapper (cause), a success test of **grew by exactly 1 AND newest capture dated today** (detection, and it stands alone if the lock is bypassed), and a **content guard** in `expectation_snapshot.py` that refuses a same-day identical payload and exits 2, reported DUP not FAIL. The guard is scoped to the DAY, not to content alone: an identical payload a week later is a real observation (a quiet week is a finding), and `test_dup_guard.py` pins all four cases offline. The duplicate was removed and the pre-dedupe file kept at `.state/expectation_snapshots.pre_dedupe_2026-08-17.json`; 3 distinct captures remain. Found by a freshness checker written for a different purpose — the count-vs-content distinction only became visible once something hashed the rows |
| C37 | `download_stock_futures.py` reported 1,950 futures bars saved, and the lot-size work was about to `UPDATE` `contract_size` on them | **both wrote to the repo-local MIRROR, which is READ-ONLY BY POLICY — the source of truth is the Drive copy and neither touched it** | `db_config.resolve_writable_db_path()` exists for exactly this and its docstring names the exact case: *"The SQLite path a WRITER may open. Never the repo-local copy. If the Drive mount is missing — signed out, not yet synced, RUNNING IN A SANDBOX — a writer must FAIL rather than quietly write into the local read-only copy and manufacture the divergence this module exists to prevent."* The new downloader hardcoded `os.path.join(_ROOT, "option_chains.db")` instead, so a month of single-stock futures landed in a copy that nothing downstream treats as authoritative, and the run printed success. Audit of every DB path in the repo: **5 modules use the resolver, 11 hardcode the mirror** — of which the `backend/quant/*` ones are READERS and legitimate (reading is what the mirror is for), while the two new WRITERS were not. The rule is now explicit: writers resolve, readers may use the mirror but inherit its staleness. FIXED by routing the downloader through the resolver, lazily so `--dry-run` still works where Drive is absent — verified both ways, dry-run passes and a real write now refuses loudly. The `--write` mode of `lot_sizes.py` is DELETED rather than repointed: a later pass mutating rows was the wrong place for the value, so the writer sets `contract_size` at capture time from the scrip master instead, and that file now opens the database `mode=ro`. Related self-inflicted damage: the failed `UPDATE` left a hot rollback journal that made the mirror unopenable until SQLite could roll it back, because the device bridge forbids `unlink`. Found by the user saying not to change the database because it is only a local copy |
| C38 | MOFSL's Nifty Q1 FY27 PAT growth of **+18%** filed under C31 as 'a wider universe, not comparable' | **the universe is essentially the SAME. The BASIS differs, and MOFSL's exhibits let this be checked NAME BY NAME — the first external validation the panel has had** | Exhibit 26 prints per-company YoY PAT for the whole Nifty. Compared against our 47-name panel: **31 of 45 comparable names agree within 5pp**, many exactly — ASIANPAINT 40/40, APOLLOHOSP 34/34, TECHM 28/28, TATACONSUM 28/28, KOTAKBANK 26/26, LT 14/14, BAJAJFINSV 12/12, COALINDIA 1/1. **RELIANCE settles the basis question outright: reported we read -22.4%, ex the 8,924 cr Asian Paints gain (C33) we read +15.9%, and MOFSL prints +16%.** That is a 0.1pp match on the one name where the adjustment is known, so the reported-vs-normalised explanation is now VERIFIED rather than inferred. THE WALK: +7.12% reported -> **+11.84%** normalising Reliance -> **+12.90%** also dropping ADANIENT, which Exhibit 26 does not list at all and which is our second-largest drag (885 -> -1,152 cr). **5.1pp remains unexplained.** Three named leads for it, each testable: (1) **DEFINITION VARIES BY NAME** — SHRIRAMFIN on our attributable basis is +28% but on the page's including-MI basis is exactly **+60%, MOFSL's number**, so they are not uniformly on either basis; a broker normalises per company, which is better for 'underlying' and not reproducible without their adjustment list. (2) **ONGC is the largest single gap at -91pp** (ours +21%, MOFSL +112%) and NEITHER of our bases reproduces it — attributable +21%, including-MI -43% — so a standalone basis or a base one-off is the hypothesis, untested. (3) **COVERAGE BIASES US DOWN**: JIOFIN (+180%) and NESTLEIND (+49%) are in Exhibit 26 and absent from our panel entirely, excluded by `_MIN_YEARS = 6`; both are strong positives, which raises the cost of that gate from cosmetic to material. Other unresolved two-way gaps, all O14 candidates: HINDALCO -43pp, ZOMATO -35pp, TITAN +28pp, M&M +27pp, SUNPHARMA +24pp, JSWSTEEL +17pp. **The margin exhibit corroborates too**: MOFSL's EBITDA margin ex-Financials 20.3%, -90bp YoY, against our operating cohort's 19.0%, -1.9pp — same direction, 1.3pp apart on level with different cohort rules. **CONCLUSION: both aggregates are right on their own terms.** Ours is one definition applied uniformly, which is what C24 and C32 demand of a time series; theirs is normalised per name, which is what a single quarter's 'underlying' reading wants. The gap between them is not an error to fix but a quantity to publish — and it is exactly what O14 exists to measure |

---

## 3. Data limitations that bound every result

| limitation | effect on conclusions |
|---|---|
| `chain_rows.call_iv / put_iv / call_bid / call_ask / put_bid / put_ask` are **100% empty** | slippage is unmeasurable. Every management result (#37, #38, #41) assumes 1-point slippage rather than measuring it. **Highest-value data fix in the repo.** |
| Option chain covers ~31 days of one calm regime (VIX 11.6–14.7) | the chain cannot contain a shock. Use the chain for execution/pricing; use the 8-year index history for regime questions. Never the reverse |
| `fii_dii_flows` covers **2026-06-18 → 2026-08-12** (37 rows) | FII **cash** flow cannot be tested across the drawdown. All FII conclusions in §1 are **futures only** |
| `participant_flows` is traded VOLUME, `participant_oi` is POSITION | `long − short` in the volume file is the one-day *change*, not the level. Verified: matches the level's daily change on 225/242 days; the 17 exceptions are all monthly expiries (settlement is not a trade) |
| 1-minute futures data starts **2026-06-29** | the wide-basis months (Apr–Jun, up to 30% annualised carry) exist only as daily bars and cannot be cross-validated. #28 rests on this |
| **MARKET-STRUCTURE BREAK on 2026-08-03: NSE launched the Closing Auction Session (CAS)** | Continuous trading now ends 15:15 IST; a 20-min auction sets the official close. Closes before and after that date are NOT methodologically comparable — before, the close was a last-30-min VWAP; after, it is the auction print (which is why the 1d bar and the last 1m bar diverged by up to ±20 pts before 3 Aug and are exactly equal after). Measured auction jump (15:15 print → close): mean \|10.2\| pts before, **mean \|65.6\| pts after, 9/9 POSITIVE (p=0.002), max +201**. On 3 Aug the auction alone was 51% of the day's +390. Any close-based study spanning 3 Aug 2026 inherits this discontinuity |
| `expectation_snapshots.json` holds **3 distinct captures** (2026-08-08, 08-16, 08-17) and **zero** earnings events fall after any of them | #43 is now MEASURABLE but not yet MEASURED. Three captures nine days apart with no print between them measure vendor drift, not revision around an event; the first real test is Q2 FY27, late Oct 2026. Pre-print consensus is unrecoverable after the fact, which is why the cadence matters more than the current count. Now on cron (Mon 09:00) via `run_expectation_snapshot.sh` — see C36 for why a growing count is not proof it ran |
| Constituent weights in `nifty-50-stock-list.csv` are rounded | bottom-up point contributions carry ~11% residual against the actual index move. Rankings reliable, last decimal not |
| Capture pipeline has stopped twice (2026-08-10, 2026-08-13 at 11:59 IST) | any "how the session closed" read may be a mid-session snapshot. Check `max(ts)` before trusting a same-day number |
| `.state/flows_cash_cache.json` writes `0.0` on fetch failure | for a *net position* zero is a legitimate value, so failures are indistinguishable from real flat books. 2026-07-17 was 0.0; true value −216,528 (recovered from both directions and confirmed by `participant_oi`) |
| **Breeze serves no history for SETTLED futures contracts** | a futures series can only grow FORWARD from the day capture starts — there is no retrospective build. This is what closed O12's time-series leg: 12 months of stock-futures history cannot be obtained at any price, so the FII hedge ratio cannot be computed for the 2026 drawdown that motivated the question. Anything wanting a futures panel must start capturing now and wait |
| **TWO copies of `option_chains.db` exist and only one is authoritative** | Drive (`My Drive/option_chains.db`) is the source of truth, written by the sync and backfill jobs. `<repo>/option_chains.db` is a MANUAL COPY kept so tooling without Drive access can read. Same size, different inode. Every level-dependent number in this repo comes from whichever copy the script resolved, and the mirror is only as fresh as the last `cp` — so a figure can be internally consistent and still describe a stale world. Writers must go through `db_config.resolve_writable_db_path()`; see C37 for what happens when one does not |
| **`fii_dii_flows` exists in BOTH stores. RESOLVED 2026-08-17: the Postgres copy is PRE-FIX RESIDUE and is safe to drop.** SQLite 39 rows (2026-06-18 .. 2026-08-14), Postgres 32 | Compared column by column on shared dates, and both symptoms are the SAME already-fixed bug rather than two problems. (1) Every date Postgres holds and SQLite does not is a **Sunday** — and `_flow_date()` drops weekends deliberately, its docstring recording why: *"Upstox returns a record dated SUNDAY whose payload is byte-identical to the following Monday — 7 of 41 rows on 2026-08-09, six of them exact duplicates."* Verified: SQLite has **zero** weekend rows, and the Monday following each Postgres-only Sunday is present. (2) All **126** value disagreements are the same number one day apart (23,273.71 on Mon 06-29 in Postgres, Tue 06-30 in SQLite) — one misalignment counted many times, not many conflicts. **SQLite is the correct store.** Drop the Postgres table together with the vestigial `db_url = resolve_pg_dsn()` on `download_fii_dii.py:45`, in one commit, so nobody is later sent looking for a store that no longer exists. **THE CROSS-CHECK ALSO SETTLED A SEPARATE QUESTION** it was built for: 'zero in SQLite, real in Postgres' came back EMPTY, so the all-zero F&O blocks on 2026-06-18 and 2026-06-22 are zero in BOTH stores — the F&O feed's start boundary, **not** lost fetches. Neither date needs revisiting. **WHY THIS KEPT RECURRING:** the first version of `fii_store_compare.py` read 'extra dates + disagreements' as 'the fossil holds more', and told the user to preserve and harvest work they had already diagnosed and fixed. MORE DATA and WRONG DATA are identical to a set difference; separating them needs the two signatures above, which the checker now tests explicitly. A tool that recommends re-litigating settled work is worse than no tool, and this was the third check in one session to need narrowing for that reason (see also the 24-finding first pass of `db_path_audit.py` and the APOLLOHOSP false alarm in `lot_sizes.py`) |
| **FII period labels are not all quarter-ends** — ADANIENT carries `Jul 2026`, SHRIRAMFIN `Apr 2026` | `change_pp` on those names is measured across an unknown interval, so the direction is usable and the MAGNITUDE is not. Flagged, never imputed. The label defect and the value defect below look like one bug and are not — a single repair rule would have overwritten a real vendor disagreement |
| **ICICIBANK's Jun-2026 FII stake is DISPUTED, not wrong** — our 49.82% against MarketsMojo's 33.79%, a +15.33pp quarter move | roughly 16pp is classified FII by one vendor and Other-DII by another; Upstox confirms our Mar-2026 value and Trendlyne shows the stake in a 44.8-46.2% band. **There is no arithmetic error to repair.** FII% is a flag and never a gate, precisely because vendor classification is not stable enough to select on |

---

## 4. Open — not yet tested

- **O15 — Postgres `fundamentals.financials` carries `DUMMY_ISIN_<SYMBOL>` rows alongside real ISINs for 40 of 62 companies. DO NOT DELETE THEM — they are NOT duplicates.** **RETRACTION OF A 'CORRECTION' MADE EARLIER THE SAME DAY:** this entry was edited to say it had named the wrong table — that `fundamentals` was a 5-row stub and the data was in `financials`. **That edit was wrong and is reverted.** `schema.sql:21` declares `CREATE SCHEMA IF NOT EXISTS fundamentals` and every table is schema-qualified — `fundamentals.companies`, `fundamentals.financials`. The original shorthand was correct. The error came from reading `relname` out of `pg_stat_user_tables`, **which does not carry the schema**, and inferring table identity from a bare name. That is the same failure shape as the cross-join earlier in this arc, where a comparison omitted `basis`, `time_period` and `section` and reported zero conflicts: in both cases an aggregate was read without the dimension that qualifies it. Two instances now, so the rule is worth stating — **never identify a Postgres table from an unqualified name; query `schemaname || '.' || relname`.** `pg_backup.sh` now prints schema-qualified counts so the next inventory cannot mislead the same way. Found because the user said the earlier Postgres comparison had found no problem, which prompted re-reading `schema.sql` rather than re-running the check. **What DOES survive from that inventory: `companies` = 102 = 62 real + 40 dummy and `company_profile` = 62, an independent confirmation of the 40-of-62 count that the first version of this entry got wrong by generalising from RELIANCE alone. And a table with relname `fundamentals` holding 5 rows exists somewhere outside the schema — probably `public.fundamentals` — which is a stray worth one query, and is NOT what this entry is about.** Created by `insert_missing_companies.py` and never reconciled. Both sets span 2017-2026 (real ~260 rows per company, dummy ~70), so any aggregate joining `companies` to `financials` double-counts unevenly. **CORRECTION TO THE FIRST VERSION OF THIS ENTRY**, which said the dummies were a strict subset holding identical values and were safe to drop: that was generalised from RELIANCE alone. Scanned across all 40 symbols there are **61 conflicting rows**, and they are not errors — they are a SECOND CONVENTION. On BAJAJ-AUTO, checked against our independent page scrape: **revenue matches the DUMMY** (Jun-2026 21,688.83 vs real 22,376.69 vs ours 21,689 — the real row looks like total income including other income) while **net_profit matches the REAL** (3,188.75 vs dummy 3,225.63 vs ours 3,189). Split cleanly by line item. The dummy's `EPS - Diluted` also carries full float precision (214.1416961130742), so it is DERIVED, not as-reported. Deleting the dummy rows would destroy the revenue series that agrees with an independent source. Any cleanup must RECONCILE the two conventions per line item, not drop one side. The join used to detect this must include `basis`, `time_period` AND `section`; omitting them cross-joins consolidated against standalone and manufactures conflicts that are not there — which is how the first check read zero. Symptoms that exposed the duplication: a Jun-2026 coverage query returned **59 symbols present AND 12 missing out of 62**, three listed twice, and `net_profit` showed **96 rows at one period for 59 claimed companies**. That 59 was briefly misread as Postgres having BETTER coverage than the JSON panel; it does not — APOLLOHOSP and GRASIM stop at 2026-03-31 on their real ISINs, the same late reporters that pinned the export panel at 37 names, from the same 15-Aug workbooks. CONSUMER: the IT/Bank thesis engines only — `backend/quant/` never imports psycopg2, so the Nifty50 view is unaffected and no correction from C26-C34 propagates here. If those engines are dormant this is a defect with no consumer. NOTE: this schema models `basis` and `time_period` as first-class columns — the distinction C32 had to rediscover — so it is the better target if `ingest_screener.py` is ever re-pointed at `screener_page_tables.csv` for 13 quarters and the lender template.

- **O14 — a one-off / corporate-action register, per company per year.** C33 shows FY26 growth ranges +4.63% to +9.01% depending which exceptional you strip, and that was found by hand on two names. There is no systematic detection: `delivery_history` carries `depressed_base` and `share_move_flags` but neither is used in the aggregate, and Screener's P&L does not isolate exceptional items cleanly. Needs a maintained list — demergers, deconsolidations, stake sales, one-time tax items — with the amount and the year, so a reported-vs-normalised growth pair can be published side by side instead of one number that happens to be central. Until then every growth rate in this repo is REPORTED, not underlying, and should be read that way.


- **O12 — net the FII derivatives book in NOTIONAL, not contracts. CLOSED 2026-08-17: PARTLY ANSWERED, REST UNMEASURABLE.** What survives is a point-in-time measurement. Breeze reports futures open interest and volume in SHARES rather than contracts, so notional is `open_interest x close` with no lot-size table required — established three ways, not assumed: the GCD of all OI observations per contract is a clean lot for every name, `gcd(volume)` returns the same divisor independently, and `gcd` of the day-to-day OI CHANGES equals `gcd` of the levels on 100/100 contracts, which a coincidental common factor in the levels could not survive. Cross-checked against the exchange's own contract list (`SecurityMaster.zip` / `FONSEScripMaster.txt`): **100/100 agree**, including ADANIENT's 309, where divisibility alone is ambiguous (309 = 3 x 103, both divide every observation) and only SEBI's Rs 5-10 lakh contract-value band separates them. Front-month single-stock futures OI on 14-Aug-2026: **Rs 225,983 cr** across 50 names (HDFCBANK 23,147 - RELIANCE 14,037 - ICICIBANK 12,886).
  **WHY THE REST IS CLOSED RATHER THAN OPEN — two independent blockers, both hard.** (1) That figure is TOTAL market OI, not FII OI. `participant_oi` reports FII positions in AGGREGATE only, with no per-underlying breakdown by participant category, so converting it into an FII stock-futures long requires ASSUMING FII's share of each stock's book — a precise number resting on a guess, which is the failure this register exists to catch. (2) Even granted a composition, the hedge ratio H_t is a TIME SERIES, and Breeze serves no history for settled contracts, so the 12 months the test needs cannot be assembled retrospectively at all. Decision: **leave it.** The daily `--live` capture is cheap and keeps accumulating, so the series becomes buildable prospectively — roughly a year from now — but nothing about the 2026 drawdown can be tested. The standing statement is therefore unchanged and now permanent for that period: **'FII are net short' is true of the INDEX LEG ONLY**, and the manipulation hypothesis stays rejected on the sequencing evidence in H63 rather than on a hedge ratio.
- **O13 — extend `fii_dii_flows` backwards.** The gross buy/sell columns exist and are populated but the table only covers 18-Jun-2026 onward (38 rows). The 0.22% net/gross finding rests on two months. NSE publishes this daily and historically; backfilling it would let H62 be tested across the 2026 drawdown rather than after it.

- **O10 — which crude channel dominates: earnings or valuation/macro?** The right test is: after a crude shock, what happens separately to EPS revisions, the multiple, INR, bond yields and realised vol. BLOCKED, and specifically: the reconstructed index P/E steps only ONCE A YEAR, so over a 60-session window 'P/E change' is almost exactly 'price change' and the decomposition is circular. A first pass (60d crude run ≥+15%, 60 forward sessions) gives Nifty +5.0% / P/E −1.2% / INR +0.55% / VIX −8.4% versus a crude slump's +5.6% / +4.6% / −0.25% / −20.8% — directionally consistent with a valuation channel, on **6 and 3 independent windows**, and contaminated by the annual step. Needs a quarterly TTM earnings series; Screener's export gives only 10 quarters, so this cannot run before ~2027 without another source. Bond yields are not in `price_bars` at all.
- **O11 — US tariffs as an evidence channel.** Not in the `earnings_acceleration.py` channel taxonomy and no data wired. Would hit export-facing IT, pharma, autos and textiles through volumes, not input costs. Currently unmeasured and unclaimed.

- **H-EARN-ACCEL — does aggregate Nifty-50 earnings growth reaccelerate from ~3.7% toward the 12-14% assumed for FY27?** THE open index-level question, and the one the valuation work led to: at roughly today's multiple the two assumptions are ~1,150 Nifty points apart over a year (26,047 vs 27,200), which dwarfs anything the cheap/expensive debate turns on. Gap to the band: **8.3pp**. Settles on four consecutive quarters of aggregate PAT growth; next observation Q2 FY27, late Oct 2026. Four of eight evidence channels are wired; the highest-value one — direction of consensus EPS revisions — is BLOCKED on holding more than one `expectation_snapshots.json` capture. Put `expectation_snapshot.py` on a weekly schedule and it unblocks.

- **O9 — Does a low starting index multiple predict higher forward returns?** PARTIALLY ANSWERED by H53, and not in the direction the naive tercile table suggested. The multiple did NOT revert: from a below-median start it rose in only 42-43% of 6M/1Y windows and 25% of 2Y windows. Cheap-start windows still delivered good PRICE returns — but through earnings growth outrunning a still-compressing multiple. What remains open is whether that holds outside 2018-2026, which contains exactly one regime. Needs pre-2018 history (`nifty_history_long.csv`).

---

## 5. Standing notes

- **The `cyclical` flag mislabels Dr Reddy's** (patent-exclusivity cliff, not a cycle). Known, unfixed.
- **Two-database rule:** chain for execution/pricing where it is authoritative; 8-year index history for regime and entry-timing questions.
- **Position advice:** this repo produces base rates and factual analysis. It does not produce recommendations on live positions.

| C39 | `SCALE = {"USDINR": 10.0}` in `sync_commodities.py`, applied unconditionally to every USDINR candle | **a compensation for someone else's convention, asserted once as a constant — and the only party who can change that convention is the one we cannot see** | Upstox quoted `GLOBAL_INDICATOR|USDINR` 10x scaled and the script divided by 10. On **2026-08-16 Upstox stopped scaling it**. Our divisor did not change — `git show 22e237e^` confirms the same `/10` in the pre-refactor file, so the 15-Aug consolidation moved it and did not touch it — and the compensation became the defect. Proof needs no vendor documentation: the last 1m close before the flip is **95.415** and the first after it is **9.5415**, the same price, **ratio 10.0000 to four places** on a rate that moves ~0.05% a day. BLAST RADIUS WAS LARGER THAN THE FLIP: `_resume_from(..., overlap_days=5)` re-fetches the last five sessions every run and `write_daily` upserts them, so the first bad run REPLACED five already-correct daily bars — 08-09, 11, 12, 13, 14 and 16 all wrong, 08-07 and earlier untouched. The overlap that exists to catch vendor REVISIONS propagated a vendor DEFECT backwards. WHY NOTHING CAUGHT IT: `implausible()` is the guard for exactly this failure and it reads `PLAUSIBLE_1M_FLOOR`, which covers GOLD, SILVER, COPPER and CRUDEOIL_MCX and **has no USDINR entry** — so it returned None and the store wrote. A floor is also only half a guard: a 100x flip the other way satisfies it. FIXED by deleting the constant and stating what the series IS — `PLAUSIBLE_BAND = {"USDINR": (50, 150)}` — then inferring the power of ten that puts each RESPONSE inside that band, re-derived every run. Powers of ten only and a band this narrow, so it can correct a decimal point and cannot rescale a wrong instrument into looking right: GIFTNIFTY's 24,393 under the USDINR key reaches no power of ten inside 50-150 and is REFUSED, while GOLD at 155,940 is untouched because it has no band. Verified on six synthetic cases (pre-flip 954.15, post-flip 95.415, hypothetical 9541.5, wrong instrument, MCX untouched, GOLD floor still firing). Data repaired by `fix_usdinr_scale.py` — selects on `close < 20` (the 2018 low is 63.26, so there is no date boundary to get wrong), multiplies OHLC by 10, and proves the repair by CONTINUITY across the boundary rather than by a row count, which is C36's lesson. Found while checking whether MCX gold prices reconcile to COMEX parity — the FX leg was the broken one |

| C40 | `TARGET_BAND_PCT = (12.0, 14.0)` in `earnings_acceleration.py`, recorded as **the** sell-side FY27 assumption the H-EARN-ACCEL gap is measured against | **that was ONE broker. A second house publishes 10%, below the band — so the band was not a consensus, it was a sample of one, and the gap to our run-rate was drawn wider and straighter than the evidence supports** | BofA Global Research, 2026-08-18, raised FY27 index earnings growth to **10% from 8.5%** on June-quarter resilience, citing firm GST collections, direct tax receipts, credit growth and power demand through the West Asia conflict. That lands INSIDE our own range — **+7.12% reported, +11.84% ex the Reliance one-off** — and BELOW the 12-14% this file was testing against. Set beside C38 it is the second independent confirmation of the basis story: brokers publish NORMALISED, we publish REPORTED, and once that is allowed for the houses are not far apart (BofA 10, Axis 12-14, MOFSL 18, market-implied 26.9). FIXED by widening the band to **(10.0, 14.0)** and naming both houses in TARGET_SOURCE. **THE TARGET ARITHMETIC IS MORE CONSERVATIVE THAN ITS OWN NARRATIVE**: BofA call 26,200 by Dec-2026 'assuming no further expansion in valuations', but on our index EPS of 1,210 a 10% year gives 1,331 and today's 20.14x would put the index at **26,806**. Their 26,200 implies **19.7x — a mild DE-rating**, so published narrative and published arithmetic disagree by ~2%. Their bear at 22,000 implies 18.2x on flat EPS, still above the **16.56x** minimum in the 2,001-session sample, so even the bear case sets no new low. Both are now recorded in `nifty_outlook.py` under the same quoted/assumed discipline as the Axis scenarios, with exit_pe marked ASSUMED because BofA published a level and a narrative, not a multiple. Minor internal inconsistency logged: '9% upside to 26,200' implies a spot of 24,037 and '8% downside to 22,000' implies 23,913 — 0.5% apart, and neither is a recent close (24,155 on 18-Aug, 24,288 on 17-Aug; the 18-Aug intraday LOW was 24,058). **SECTOR CALLS TESTED AGAINST THE PANEL** (Q1 FY27 attributable YoY): NBFCs **+22.2%** vs large private banks **+12.3%** SUPPORTS the preference, PSU **+6.6%** supports the caution, autos **+20.3%** and cement **+30.1%** support both overweights. Healthcare **-12.3%** (DRREDDY -69, CIPLA -39) supports the underweight on a STRONGER basis than their valuation argument. IT is the informative one: **-20.2% YTD on earnings of +10.5%**, above the panel's +7.1% — a pure de-rating, which argues for the upgrade better than 'a sharp correction' does. Telecom runs the other way on trailing data (BHARTIARTL **+25.5%**); their case is forward and our panel cannot see it. Real estate's 'sharp re-rating' is **NOT SUPPORTED** — NIFTYREALTY +3.5% YTD and +1.1% over a year, though ~11pp ahead of the index. NOT CHECKABLE HERE: the '71% of market cap' framing (no weights mapped to their taxonomy), the volume claims, and — the one that matters — **their central recommendation, since mid-sized private banks are not in the Nifty 50**. Metals is absent from their note and is our best 1-year performer (+39.6%) on Q1 FY27 earnings of +33.9% |

---

*Last updated 2026-08-18 (C40: the sell-side band was one broker; BofA at 10% falls below it and inside our reported-to-normalised range, so it is widened to 10-14% and both houses are named. Their 26,200 implies a mild de-rating, not the flat multiple the note claims. C39: Upstox stopped 10x-scaling the USDINR indicator on 16-Aug and our unconditional `/10` became the defect; the 5-day resume overlap then rewrote five previously-correct daily bars. Constant replaced by a per-response inferred divisor inside a plausibility BAND, and `implausible()` now checks both ends. C38 VERIFIED name-by-name against MOFSL's Exhibit 26: 31 of 45 names within 5pp, and Reliance ex-one-off +15.9% against their +16% settles the reported-vs-normalised question to 0.1pp; 8 of 9 named names agree and Reliance's one-off explains the sign flip. C31's 'different universe' corrected). Previously 2026-08-17 (`fii_dii_flows` two-store question CLOSED: the Postgres copy is pre-fix residue — Sunday rows and a uniform one-day offset, both the bug `_flow_date()` already fixes — and the same comparison confirmed the two all-zero F&O dates are a feed boundary, not lost fetches. Postgres inventoried via the new backup. One real finding — `fii_dii_flows` exists in both stores with different row counts, the Postgres copy an unread fossil — and one self-inflicted error: a 'correction' to O15's table name, made from a schema-blind `relname` list, retracted the same day). Also 2026-08-17 (C37: two new writers bypassed `resolve_writable_db_path()` and wrote to the read-only mirror; O12 closed — expired futures are unobtainable, so the hedge ratio cannot be built retrospectively). Also 2026-08-17 (C35: 5y CAGR inherits its starting year's cycle — median annual growth published beside it. C36: the weekly capture wrapper proved success by a counter and a duplicate write satisfied it; lock + content guard + `freshness.py`). Previously 2026-08-16 (C26-C34: margin panel rebuilt; quarterly panel rebuilt on attributable profit — exit rate +3.7% -> +7.1%, path turned up). Add a row when a test finishes — including when it fails.
A register that only contains successes is a register that will re-run every failure.*
